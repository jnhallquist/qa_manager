{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.11.0",
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "axios": "^0.19.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1"
  },
  "devDependencies": {
    "cypress": "^4.9.0",
    "prettier": "2.0.5",
    "start-server-and-test": "^1.11.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:ci": "CI=true react-scripts start",
    "build": "react-scripts build",
    "cypress:run": "start-server-and-test start http://localhost:3000 cypress run",
    "ci:cypress": "cypress run --record --key=${CYPRESS_RECORD_KEY}",
    "ci:cypress:run": "start-server-and-test start http://localhost:3000 ci:cypress:run",
    "test:unit": "CI=true react-scripts test --env=jsdom",
    "test": "yarn test:unit && yarn cypress:run",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
